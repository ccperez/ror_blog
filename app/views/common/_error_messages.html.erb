<% if object.errors.any? %>
	<h2><%= pluralize(object.errors.count, "error") %> prevented this <%= object.class.to_s.downcase %> from saving:</h2>
		<ul>
			<% object.errors.full_messages.each do |msg| %>
				<li><%= msg %></li>
			<% end %>
		</ul>	
<% end %>